source 'https://rubygems.org'

# The ruby version is installed by the Docker file in /docker/Dockerfile
ruby "2.3.0"

group :development do
  gem 'rake'
  gem 'nokogiri'
  gem 'elasticsearch'
end

group :test do
  gem 'rspec'
  gem 'webmock'
  gem 'pry'
end
